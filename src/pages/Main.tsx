import React, { FC } from 'react'
import CityCard from '../components/CityCard/container'

const MainPage: FC = () => {
  return (
    <div>
      <CityCard />
    </div>
  )
}

export default MainPage
